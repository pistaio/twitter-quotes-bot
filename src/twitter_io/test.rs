// use crate::twitter_io::*;

#[test]
fn quote_short() {
    let quote = "This is a short quote".to_string();
    // let converted_tweet = convert_to_tweet(quote.clone());
    // assert_eq!(vec![quote], converted_tweet);
}
